@model IEnumerable<GestaoCompras.Domain.Entities.ClasseProduto>
@{
    ViewBag.Title = "Lista de Classes de Produto";
}

<h2>Classe de Produtos</h2>

<div class="panel panel-default">
    <div class="panel-heading text-center">
        <strong>@ViewBag.Title</strong>
    </div>
    <table class="table table-hover">
        <tr class="panel-heading">
            <th>
                @Html.DisplayNameFor(model => model.NomeClasse)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NomeClasse)
                </td>
                <td>
                    <a href="@Url.Action("Edit", "ClasseProdutos", new {id = item.ClasseProdutoId})" class="btn btn-default"><span title="Editar" class="glyphicon glyphicon-pencil"></span></a>
                    <a href="@Url.Action("Details", "ClasseProdutos", new { id = item.ClasseProdutoId })" class="btn btn-default"><span title="Detalhes" class="glyphicon glyphicon-search"></span></a>
                    <a href="@Url.Action("Delete", "ClasseProdutos", new { id = item.ClasseProdutoId })" class="btn btn-danger"><span title="Excluir" class="glyphicon glyphicon-trash"></span></a>
                </td>
            </tr>
        }

    </table>
</div>

<div>
    <div class="form-group">
        @Html.ActionLink("Inserir", "Create", "ClasseProdutos", null, new { @class = "btn btn-primary" })
    </div>
</div>

